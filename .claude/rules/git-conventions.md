---
description: Git操作（ブランチ作成・コミット・PR）を行う際のルール
globs: []
---

# Git規約

## ブランチ命名

- フォーマット: `{type}/{issue番号}-{英語の短い説明}`
- type: `feat`, `fix`, `refactor`, `ui`, `docs`, `chore`
- 例: `feat/12-add-child-profile`, `fix/8-auth-redirect`

## ブランチ作成手順

新しいブランチを作成する前に、必ずmainを最新化する:

1. `git checkout main`
2. `git pull --ff-only origin main`
3. `git checkout -b {type}/{issue番号}-{英語の短い説明}`

※ worktree使用時は `git fetch origin main` の後に `git worktree add <path> -b {type}/{issue番号}-{英語の短い説明} origin/main` で最新のmainからブランチを作成する

## コミットメッセージ

- プレフィックス必須: `feat:`, `fix:`, `refactor:`, `ui:`, `docs:`, `chore:`, `test:`
- 日本語での記述可
- 例: `feat: 子どもプロフィール追加機能を実装`

## コードレビュー（セルフレビュー必須）

### 実施タイミング

- **すべてのコミット前にセルフレビューを必ず実施する**
- 自動コミット時も例外なく実施する（「自動コミットルール」参照）

### セルフレビュー手順

1. `git status` で未追跡ファイル・未ステージの変更を確認
2. `git add <対象ファイル>` でコミット対象をステージング
3. `git diff --staged` でコミット対象の差分を確認
4. 以下のチェックリストを順番に確認
5. 問題があれば修正してから再度レビュー（手順1からやり直し）
6. すべてOKならコミットを実行

### セルフレビューチェックリスト

#### 基本チェック

- [ ] **可読性・保守性**: コードは読みやすく、意図が明確か
- [ ] **型安全性**: TypeScriptの型定義は適切か（`any`を避けているか）
- [ ] **セキュリティ**: APIキーの露出、RLS設定漏れ、要配慮個人情報の暗号化漏れはないか
- [ ] **既存コードとの整合性**: 命名規則、ディレクトリ構造、コーディングスタイルは統一されているか
- [ ] **デバッグコード削除**: 不要な`console.log`、コメントアウトしたコード、TODO残りはないか
- [ ] **コミットメッセージ**: 変更内容を適切に表しているか（プレフィックス付き）

#### ロジック・品質チェック

- [ ] **エッジケース検証**: null/undefined、空配列/空文字列、境界値（0、負数、最大値）、同時実行・競合状態を考慮しているか
- [ ] **エラーハンドリング**: 外部API呼び出し・DB操作・ファイルI/Oの失敗パスが網羅されているか。エラーメッセージは原因特定に十分な情報を含むか
- [ ] **テスト十分性**: 正常系に加え、異常系（不正入力、タイムアウト、権限エラー）と境界値のテストがあるか。モック対象が実装と乖離していないか
- [ ] **設計妥当性**: 各関数/モジュールの責務は単一か。依存方向は上位→下位か。公開インターフェースは必要最小限か

#### 波及チェック（レビュー連鎖防止）

変更が関連箇所に矛盾を生んでいないかを確認する。このチェックを怠ると、Copilotレビューで「修正→新たな指摘→再修正→…」の連鎖が発生する（実績: 平均4.2ラウンド/PR）。

- [ ] **同一ファイル内の類似パターン**: 修正した表現・構造・変数名と同じパターンが同一ファイル内に他にもないか（grep/検索で確認）
- [ ] **cross-reference整合性**: 変更したファイルを参照・引用している他のファイルに矛盾がないか（例: ルールファイル間の相互参照パス、設定ファイルとドキュメントの記述）
- [ ] **PR説明文との整合性**: コミットメッセージやPR説明文に記載した内容（「4セクション構成」「OWASP Top 10」等）が実際の変更内容と一致しているか
- [ ] **コード例・コマンド例のロバスト性**: 追加・修正したコード例やシェルコマンド例が構文的に正しく、コピペしてもエラーにならないか（`{ ... }` → `{ /* ... */ }`、`echo` → `printf`、終了コード処理等）

### コミット前の出力フォーマット（必須）

**コミット実行前に必ず会話の中で以下の形式で出力すること：**
（Claude Codeがコミット前にユーザーに見える形で出力する）

```
セルフレビュー実施:
[基本チェック]
- [x] 可読性・保守性: （確認内容を記載）
- [x] 型安全性: （確認内容を記載、該当なしの場合は「N/A」）
- [x] セキュリティ: （確認内容を記載）
- [x] 既存コードとの整合性: （確認内容を記載）
- [x] デバッグコード削除: （確認内容を記載、該当なしの場合は「N/A」）
- [x] コミットメッセージ: （確認内容を記載）
[ロジック・品質チェック]
- [x] エッジケース検証: （確認内容を記載、該当なしの場合は「N/A」）
- [x] エラーハンドリング: （確認内容を記載、該当なしの場合は「N/A」）
- [x] テスト十分性: （確認内容を記載、該当なしの場合は「N/A」）
- [x] 設計妥当性: （確認内容を記載、該当なしの場合は「N/A」）
[波及チェック]
- [x] 同一ファイル内の類似パターン: （確認内容を記載、該当なしの場合は「N/A」）
- [x] cross-reference整合性: （確認内容を記載、該当なしの場合は「N/A」）
- [x] PR説明文との整合性: （確認内容を記載、該当なしの場合は「N/A」）
- [x] コード例・コマンド例のロバスト性: （確認内容を記載、該当なしの場合は「N/A」）
```

この出力がない状態でのコミットは禁止。

## PR規約

- `Closes #XX` でIssueをリンクする
- 本文に概要・変更内容・テスト方法を含める
- assigneeに `{github_username}` を設定する
- reviewerを設定する（`gh api` を使用）

## 自動コミットルール

- 作業の論理的な区切りごとに、**ユーザー確認なしで**自動的にコミットする（ただしセルフレビューは必須）
- **自動コミット時もセルフレビューを必ず実施する**（上記「セルフレビューチェックリスト」「コミット前の出力フォーマット」参照）
- 以下の場合のみユーザーに確認を求める:
  - mainブランチへの直接コミット（`/release` コマンドによるリリースコミットを除く。これは禁止事項だが万が一その状況になった場合は、必ずユーザー確認を行う）
  - 既存コードの大幅な削除や書き換え（ファイル削除、50行以上の削除等）
- それ以外（設定ファイル変更、新規ファイル追加、通常の編集、および `/release` コマンドによるリリースコミット）は全て自動コミット（ユーザー確認不要）

### 自動コミットの流れ

1. 変更作業を実施
2. `git status` で変更内容を確認
3. `git add <対象ファイル>` でコミット対象をステージング
4. `git diff --staged` でコミット対象の差分を確認
5. **セルフレビューチェックリストを確認**（必須）
6. 問題があれば修正してから再度レビュー（手順2からやり直し）
7. すべてOKならコミット実行

## コミット粒度ガイドライン

- 1つの変更目的 = 1コミット
- 以下は別コミットに分ける:
  - 新規ファイル追加 vs 既存ファイル修正
  - 機能実装 vs リファクタリング
  - 本体コード vs テストコード
- 関連する複数ファイルの変更は1コミットにまとめてOK（例: コンポーネント + 型定義）

## 禁止事項

- mainブランチへの直接コミット禁止（必ずブランチを切ってPR経由）
  - **例外**: `/release` コマンドによるリリースコミット（CHANGELOG更新 + タグ作成）はmainへの直接コミットを許可する
- `--force` push禁止
- `--no-verify` 禁止
